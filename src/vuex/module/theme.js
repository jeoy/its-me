import actions from '../action/theme';
import getters from '../getter/theme';
import mutations from '../mutation/theme';

const state = {
    currentTheme: '#248f97'
};

export default {
    state,
    mutations,
    actions,
    getters
};
